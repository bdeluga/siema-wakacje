sqlite3 Project.db
create table city(cityid int PRIMARY KEY,name text,iso text,lon float,lat float);
create table place(id int PRIMARY KEY,name text, rate int,kinds text,wikidata text,cityid int, lon float,lat float,
FOREIGN KEY (cityid) 
REFERENCES city(cityid) 
);
create table user(userid int PRIMARY KEY,login text, password text, email text, picture text);
create table list(id int PRIMARY KEY,userid int,name text,data text,
FOREIGN KEY (userid)
REFERENCES user(userid)
);
